import { Butterfly_Kids, DM_Sans, Space_Grotesk } from "next/font/google";

export const dmSans = DM_Sans({
    subsets: ["latin"],
    display: "swap",
    preload: true
});

export const spaceGrotesk = Space_Grotesk({
    subsets: ["latin"],
    display: "swap",
    preload: true
});

export const butterflyKids = Butterfly_Kids({
    subsets: ["latin"],
    display: "swap",
    weight: "400",
    preload: true
});
